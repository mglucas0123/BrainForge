---
description: "Compress the project and user memory to save tokens"
---

# /compress-context

Run `compress-context` skill now (manual, force both files):

1. Read `.brainforge/memory/.context.md` and `.brainforge/memory/.user.md`.
2. Compress per `.brainforge/core/skills/compress-context/SKILL.md`.
3. Validate; write only if OK.
4. Report reduction per file in **pt-BR**.

Opt-out: `sem auto-compress`. No backup.
