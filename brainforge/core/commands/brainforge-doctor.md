---
description: Health check for BrainForge kit — RTK, memory, skills, optional infra.
---

# BrainForge Doctor

Run skill `brainforge-doctor` **now**:

1. Execute checks in `brainforge/core/skills/brainforge-doctor/SKILL.md` (paths: `brainforge/memory/`, `.cursor/` after sync).
2. Report PASS / WARN / FAIL table in **pt-BR**.
3. If any FAIL: list exact fix. Do not fix unless user asks.

**State:** diagnostic only unless user requests repairs.
